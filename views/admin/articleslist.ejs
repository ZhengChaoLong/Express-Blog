<!DOCTYPE html>
<html>
  <head>
    <title><%=title%>文章列表页</title>
    <link rel='stylesheet' href="//cdn.bootcss.com/bootstrap/3.2.0/css/bootstrap.min.css"  />
    <style type="text/css">
    a{margin-left: 20px; text-decoration: none;}
    a:hover{text-decoration: underline;}
    body{ padding-top:50px; }
    </style>
  </head>
  <body>
<a href="/admin/home">返回主页</a>
<h1><%=title%>文章列表页</h1>

<input  type="submit" class="btn btn-info"  value="发布新文章">

<% if(!articles.length){ %>
<h3>亲～，该分类下没有文章哦～</h3>
<%  }else{  %>
    <table class='table'>
    <tr>
        <th>文章标题</th>
        <th>类别</th>
        <th >操作</th>
    </tr>
    <%  articles.forEach(function(article){  %>
      <tr>
      <td><%= article.title %></td>
      <td><%= article.cname %></td>
      <td><a href="/list/<%= article.id %>"><%= article.title %></a></td>
      <td><a href="/admin/articles/<%= article._id %>"><button type="button" name="delete" class="btn btn-info" >修改</button></a>
      <a href="/admin/articles/category/delete/<%= article.cname %>/<%= article._id %>" onclick="confirm('你确定要删除吗？');"><button type="button" name="delete" class="btn btn-danger" >删除</button></a></td>
      </tr>
    <% }) %>
    </table>
    <% } %>
  </body>
</html>