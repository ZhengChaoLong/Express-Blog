<% include public/head %>
<body>
<% include public/nav %>

<% if(!comments.length){ %>
<h3>亲～，还没有任何评论册哦～</h3>
<%  }else{  %>
<table class="table table-striped table-hover text-left" style="margin-top:40px">
    <thead>
    <tr>
        <th class="col-md-2">文章标题</th>
        <th class="col-md-3">评论内容</th>
        <th class="col-md-2">评论人</th>
        <th class="col-md-3">评论时间</th>
        <th class="col-md-2"></th>
    </tr>
    </thead>
    <tbody>
<% comments.forEach(function(comment){  %>
    <tr>
        <td><%= comment.comtext %></td>
        <td><%= comment.comtext %></td>
        <td><%= comment.comtext %></td>
        <td><%= comment.comtime %></td>
        <td>
            <a href="/admin/comment/delete/<%= comment._id %>" onclick="confirm('你确定要删除吗？');"><button class="btn btn-warning btn-xs">删除</button></a>
        </td>
    </tr>
 <% }) %>
    </tbody>
</table>
<% } %>
  <% include public/foot %>